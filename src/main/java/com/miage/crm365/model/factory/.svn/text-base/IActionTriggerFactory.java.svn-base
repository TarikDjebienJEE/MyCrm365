package com.miage.crm365.model.factory;

import com.miage.crm365.model.entity.ActionTrigger;


/**
*
* @author rudy stienne
* fabrique d'ActionTrigger
*
*/
public interface IActionTriggerFactory {

	/**
	 * instancie un objet ActionTrigger
	 * @return un objet Action
	 */
	ActionTrigger createActionTrigger();

	/**
	 * mapper pour un objet ActionTrigger
	 * @param actionTrigger l'action a mapper
	 * @param actionId
	 * @param eventParameterId
	 * @param eventParameterClause
	 * @param eventParameterTrigger
	 */
	void setAll(ActionTrigger actionTrigger,Long actionId,Long eventParameterId,String eventParameterClause,String eventParameterTrigger);
}
